# Estado de execução — 11/09/2026

## Concluído e verificado

- Inventário da VPS e do Windows revalidado sem alterar os 17 containers existentes.
- API, autenticação, sessões, sinalização e contrato de controle implementados.
- Gateway WebRTC/scrcpy implementado com H.264, Opus, PLI/FIR e watchdog de toque.
- Cliente iOS nativo criado para iOS 16+, com aspect fit, safe area, rotação, áudio, multitoque e texto Unicode via clipboard.
- Testes Go, corrida de dados e `go vet` passam no Windows e no GitHub Actions.
- A compilação Swift para simulador e dispositivo `arm64` passou no Xcode 16.4; a IPA sem assinatura foi empacotada e teve o SHA-256 validado.
- A IPA `PNHX` foi assinada pelo Sideloadly e instalada no iPhone; login, sinalização e conexão WebRTC foram observados na VPS.
- Um crash inicial foi identificado pelo relatório TCC do próprio iPhone e corrigido com a descrição de uso de áudio no `Info.plist`.
- Dependências e arquivos binários baixados tiveram checksum validado.

## Evidência atual do executor na VPS

- A instalação histórica do AVD `AndroidLocal` não foi localizada nos caminhos atuais.
- BlueStacks está instalado, porém nenhuma instância Android estava ativa e `adb devices` retornou vazio.
- O Windows informa `HyperVisorPresent=False`; não houve reinicialização ou alteração de virtualização nesta execução.
- Por decisão do proprietário, o Android ficará na VPS. ReDroid Android 14 `amd64` foi iniciado com volume persistente e ADB somente em `127.0.0.1:5556`. Após medir saturação da renderização por software em navegação, a tela foi ajustada para 576×1024; o runtime usa até 5 vCPUs e 6 GiB, com reserva de 4 GiB.
- O boot terminou (`sys.boot_completed=1`) e a captura da primeira tela funcionou.
- A renderização é por software (`angle/pastel`), pois não há KVM/GPU disponível; o RTT observado de cerca de 216 ms continua sendo risco material para o atraso.
- API e gateway estão instalados como serviços `systemd`, habilitados e ativos.
- O endpoint público `https://android.80-241-216-204.sslip.io/healthz` responde por HTTPS com certificado válido.
- O teste de fumaça validou login, criação e revogação de sessão, gateway, porta WebRTC UDP e inicialização do servidor scrcpy.
- A reinicialização do container preservou os dados do Android.

## Ainda precisa de validação real

- Reassinatura pelo Sideloadly e abertura no iPhone conectado.
- Implantação e teste da versão 1.0.3, que solicita keyframe ao conectar, usa intervalo de keyframe de 1 segundo, fixa o teto sustentável em 30 FPS, remove animações do Android, evita reinicializações repetidas da captura por PLI e separa movimentos de toque em um DataChannel não ordenado de baixa latência. O teste a 45 FPS chegou a 5–6 núcleos e causou travamentos, por isso foi descartado.
- Google Play não está presente na imagem oficial atual; Rave, WhatsApp e captura de áudio ainda precisam ser validados.
- Toque 5×5, multitoque, rotação durante gesto, texto com acentos/emoji e ausência de contatos presos.
- Medições de FPS, atraso, A/V, reconexão, duas horas e persistência por 24 horas.
- TURN e medições reais de conectividade ICE no iPhone.

Esses itens não estão marcados como aprovados. O código e a automação foram preparados para executar os testes, sem simular resultados.
